<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/mes_voitures"
        android:title="@string/mes_voitures"
        app:showAsAction="always"
        android:icon="@drawable/cars"/>

    <item android:id="@+id/mes_clients"
        android:title="@string/mes_clients"
        app:showAsAction="ifRoom"
        android:orderInCategory="2"
        android:icon="@android:drawable/ic_menu_crop"/>

    <item android:id="@+id/deconnexion"
        android:title="@string/deconnexion"
        android:orderInCategory="100"
        app:showAsAction="ifRoom"
        android:icon="@android:drawable/ic_partial_secure"/>

</menu>